<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="it.mapsgroup.gzoom.mybatis.mapper.MaterializedViewMapper">

    <select id="refreshMaterialView" >
        <if test='_databaseId == "oracle"'>
            BEGIN
            FOR r IN (SELECT mview_name
            FROM user_mviews)
            LOOP
            DBMS_MVIEW.REFRESH(r.mview_name, 'COMPLETE');
            END LOOP;
            END;
        </if>
    </select>

</mapper>